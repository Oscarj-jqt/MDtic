import { useEffect, useState } from "react";

//composant citation
function Citation({ getCitation }) {
	function fetchCitationduJour() {
		// Demande d'accès aux données de l'API
		fetch('https://api.adviceslip.com/advice')
		// l'API nous envoie uneréponse que l'on convertit en json
		.then((res) => (res.json())
		// On doit ensuite viser la citation avec : (sans l'id)
		slip.advice
		.then((date) => data.slip.advice
		// au cas où erreur
		.catch((err) => console.error(err))
		
	