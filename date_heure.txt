API
// Obtenir la date en entier en temps réel
import React, { useState } from 'react';



function getDate() {
  const date = new Date();
  today = date.toString() (en lettres)
  const month = today.getMonth() + 1;
  const year = today.getFullYear();
  const date = today.getDate();
  return `${month}/${date}/${year}`;
}

function App() {
  const [currentDate, setCurrentDate] = useState(date);

  return (
    <div>
      <h1>Today's Date</h1>
      <p>{today}</p>
    </div>
  );
}

export default App;

Date: const currentDate = new Date(); 
Puis la fonction : 
function getDate() {
  const currentDate = new Date();
  //const month = today.getMonth() + 1;
  //const year = today.getFullYear();
  //const date = today.getDate();
  return `currentDate`;
}

à partir de currentDate on obtient l'heure : 
const Time = today.getHours() + ':' + date.getMinutes() 

Il faut mettre à jour le temps à chaque minute : avec un intervalle et useEffect
Refaire la fonction précédente toute les minutes : 
const [time, setTime] = useState(new Date());

  useEffect(
    () => {
      const intervalId = setInterval(() => {
      
        setTime(new Date());
      }, 60000);
      return () => {
        clearInterval(intervalId)
      }
    } 



